[tool.poetry]
name = "www"
version = "0.1.0"
description = ""
authors = ["Kazuki Yabuuchi"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
